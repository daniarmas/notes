package test

import (
	// "context"
	"testing"
	// "github.com/daniarmas/notes/internal/customerrors"
	// "github.com/daniarmas/notes/internal/domain"
	// "github.com/daniarmas/notes/mocks"
	// "github.com/google/uuid"
	// "github.com/stretchr/testify/assert"
)

func TestAccessTokenRepository(t *testing.T) {
	// Setup
	// ctx := context.Background()
	// id := uuid.New()
	// accessTokenDatabaseDs := mocks.NewAccessTokenDatabaseDs(t)
	// accessTokenCacheDs := mocks.NewAccessTokenCacheDs(t)
	// accessTokenRepository := domain.NewAccessTokenRepository(accessTokenCacheDs, accessTokenDatabaseDs)

	// // Expectations
	// accessTokenDatabaseDs.On("GetAccessTokenById", ctx, id).Return(nil, &customerrors.RecordNotFound{})
	// accessTokenCacheDs.On("GetAccessTokenById", ctx, id).Return(nil, &customerrors.RecordNotFound{})

	// // Execution
	// result, err := accessTokenRepository.GetAccessToken(ctx, id)

	// // Assertions
	// assert.Equal(t, &customerrors.RecordNotFound{}, err)
	// assert.Nil(t, result)

	// // Verify
	// accessTokenDatabaseDs.AssertExpectations(t)
	// accessTokenCacheDs.AssertExpectations(t)
}
